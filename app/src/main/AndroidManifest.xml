<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bluetooth permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    
    <!-- For Android 12+ -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    
    <!-- Location permission required for BLE scanning -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    
    <!-- Foreground service permission for Android 9+ -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    
    <!-- Boot completed permission to start service at boot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    
    <!-- Wake lock permission to keep CPU running -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- SMS permission for emergency messages -->
    <uses-permission android:name="android.permission.SEND_SMS" />
    
    <!-- Call phone permission for emergency calls -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    
    <!-- Vibration permission for fake call feature -->
    <uses-permission android:name="android.permission.VIBRATE" />
    
    <!-- Audio settings permission for alert system -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    
    <!-- Flashlight permission for alert system -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.FLASHLIGHT" />

    <!-- Contacts permission -->
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    
    <!-- Feature declarations -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="true" />
    <uses-feature android:name="android.hardware.camera.flash" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmerBand">

        <activity 
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity 
            android:name=".EmergencyContactsActivity"
            android:label="@string/menu_contacts"
            android:parentActivityName=".MainActivity" />

        <activity 
            android:name=".SettingsActivity"
            android:label="@string/menu_settings"
            android:parentActivityName=".MainActivity" />

        <activity 
            android:name=".FakeCallActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait" />

        <activity 
            android:name=".AlertActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar"
            android:screenOrientation="portrait" />

        <!-- BLE Background Service -->
        <service
            android:name=".BLEBackgroundService"
            android:enabled="true"
            android:exported="false" />
            
        <!-- Boot completed receiver -->
        <receiver
            android:name=".BootCompletedReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
            
        <activity android:name=".CyberCellActivity"
            android:label="@string/menu_help"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".LocationSettingsActivity"
            android:label="@string/menu_location"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".CyberCellSettingsActivity"
            android:parentActivityName=".MainActivity"
            android:label="Cyber Cell Settings"
            android:exported="false" />
            
    </application>

</manifest> 